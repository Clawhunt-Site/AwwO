from __future__ import annotations

from pathlib import Path

from studio_app.application.policies import dispatch as dispatch_rules
from studio_app.application.state import defaults
from studio_app.infrastructure.paths import backend_dir


VERIFIED_DREAMY_WORKSHOP_PROJECT_ID = defaults.VERIFIED_DREAMY_WORKSHOP_PROJECT_ID
VERIFIED_DREAMY_WORKSHOP_SEGMENTS = defaults.VERIFIED_DREAMY_WORKSHOP_SEGMENTS
VALID_MODES = defaults.VALID_MODES
VALID_ACTIONS = defaults.VALID_ACTIONS
VALID_STATUSES = defaults.VALID_STATUSES
VALID_DISPATCH_TARGET_STATUSES = defaults.VALID_DISPATCH_TARGET_STATUSES
READY_AUTH_STATUSES = defaults.READY_AUTH_STATUSES
STATUS_COUNT_KEYS = defaults.STATUS_COUNT_KEYS
TERMINAL_CANCEL_STATUSES = defaults.TERMINAL_CANCEL_STATUSES
CANVASPRO_SOURCE_ASSET_MAX_BYTES = defaults.CANVASPRO_SOURCE_ASSET_MAX_BYTES
CANVASPRO_SOURCE_ASSET_MEDIA_PREFIXES = defaults.CANVASPRO_SOURCE_ASSET_MEDIA_PREFIXES
CORE_DELIVERY_PAGE_IDS = defaults.CORE_DELIVERY_PAGE_IDS
CORE_DELIVERY_AGENT_IDS = defaults.CORE_DELIVERY_AGENT_IDS
READY_GATE_STATUSES = defaults.READY_GATE_STATUSES
PENDING_DELIVERY_STATUSES = defaults.PENDING_DELIVERY_STATUSES
ISSUE_DELIVERY_STATUSES = defaults.ISSUE_DELIVERY_STATUSES
MANUAL_STUDIO_ACTIONS = defaults.MANUAL_STUDIO_ACTIONS
PLACEHOLDER_POSTERS = defaults.PLACEHOLDER_POSTERS
IGNORED_FRONTEND_ROUTE_PREFIXES = defaults.IGNORED_FRONTEND_ROUTE_PREFIXES
IGNORED_FRONTEND_ROUTE_EXACT = defaults.IGNORED_FRONTEND_ROUTE_EXACT


BACKEND_ENTRYPOINT = backend_dir() / "main.py"
DEFAULT_FRONTEND_APP_ROUTES_FILE = dispatch_rules.default_frontend_app_routes_file_for_backend(BACKEND_ENTRYPOINT)


def frontend_route_source_path() -> Path:
    return dispatch_rules.frontend_route_source_path(DEFAULT_FRONTEND_APP_ROUTES_FILE)
