{
  "name": "@awwo/pi-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.19.0" },
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test --test-concurrency=1 *.test.mjs",
    "check": "node --check server.mjs && node --check runner.mjs && node --check pi-task.mjs && node --check config.mjs"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "0.85.1",
    "@earendil-works/pi-coding-agent": "0.85.1"
  }
}
