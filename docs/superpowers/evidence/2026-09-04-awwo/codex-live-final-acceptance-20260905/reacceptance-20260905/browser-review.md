# AWW-18 浏览器证据独立复核（父事项 AWW-12）

复核日期：2026-09-05（北京时间）。结论：最新宿主证据支持原有界浏览器检查通过；实际查看的三张截图未发现阻止本轮演示交付的页面级布局问题。原先“缺少真实浏览器证据”的结论已被本次通过结果取代。本复核未重新启动浏览器，未操作事项 API，未修改应用代码。

## 回读的证据与可支持范围

证据根目录：`C:/tmp/awwo-codex-live-20260904/a784236e-c48a-468d-b14f-8392297e0452/`。

| 实际读取文件 | 观察 | 判定边界 |
| --- | --- | --- |
| `artifacts/aww-11-modal-focus/final-review.md` | 记录有界复核通过，原 modal 焦点问题已修复，报告中的 4 项源码哈希匹配，原 harness 与应用 smoke 哈希未变 | 是上一轮回读结论；本子任务不重新计算源码哈希，父代理负责当前匹配检查 |
| `artifacts/aww-11-modal-focus/final-evidence-readback.json` | `read_at_utc=2026-09-04T16:19:52.375863+00:00`；14/14，通过报告及 11 张截图均有哈希/元数据记录 | 支持证据清单与追溯；本子任务只实际查看下列 3 张截图 |
| `artifacts/aww-11-browser-evidence/results.json` | `checkedAt=2026-09-04T16:17:24.473Z`（北京时间 9 月 5 日 00:17）；Chrome `152.0.7977.82`；14 项 `passed=true`；无 `executionError` 字段 | 夹具的 320/375/768/1280px 重排、320px 的 200% 根字体压力、skip link、帮助弹窗、焦点包含/恢复、无效标题焦点与描述、状态区、强制颜色焦点样式 |
| `artifacts/browser/result.json` | `status=passed`，视口 1440/768/375/320；列有 login、skip link、published documents、native dialog Escape and focus restoration、mobile navigation Escape and focus、horizontal overflow、page errors | 实际演示应用的上述 smoke 范围；文件没有逐项 trace 或自身执行时间，不据此扩大为完整业务端到端验收 |

`results.json` 中 modal 焦点包含和 Escape 恢复现均通过。200% 检查是根字体压力测试，不是浏览器缩放。应用 smoke 与夹具应分开解释：夹具的 14 项通过不能自动证明应用所有页面或业务流程通过。

## 实际看过的截图

以下图片均通过 `view_image` 以 `detail=original` 实际查看，未加工图片。

| 截图 | 实际观察 | 结论 |
| --- | --- | --- |
| `artifacts/browser/documents-320.png`（320×2925） | 顶部明确显示模拟数据提示；导航折叠为按钮且焦点边框可见；搜索和按钮在窄屏中可见，分类/标签/集合单列排列；文档卡片、分页及分布折叠区纵向排列，未见页面级横向裁切或控件遮挡 | 支持该文档页状态在 320px 的静态布局可用；不能证明按钮行为、读屏输出或所有文档内容完整可读 |
| `artifacts/browser/documents-desktop.png`（1440×1493） | 桌面侧栏与主区域分离；当前文档空间导航清楚；搜索、筛选、创建入口和 3 列文档卡片排布完整；分页可见，无明显布局重叠 | 支持该文档页状态的桌面布局。侧栏工作区选择器的角色尾部显示被截短，非本次阻断；完整选项可读性需交互证据，截图未证明 |
| `artifacts/aww-11-browser-evidence/forced-colors-current-focus.png`（320×3706） | 当前“文档检索”链接有明显紫色外轮廓；长标题、长 URL 换行；表单与状态示例均在页面范围内；成员表格右侧内容截在其局部容器内，旁边明确提示横向浏览 | 支持夹具当前链接在强制颜色模式的可见焦点，及该状态的重排；局部表格可实际横向操作不能由静态图独立确认；不是实际应用全页强制颜色验收 |

两张应用截图的搜索框左端图标与占位文案起始字符较贴近，有轻微视觉叠压；输入框和搜索按钮本身仍完整可见，未见阻断操作的证据。此为低影响排版观察，不撤销当前有界通过结论。截图中的卡片摘要使用省略显示，不据此判为内容丢失。

## 限制及父事项采用方式

- 本子任务是现有宿主证据回读与三张截图的独立观察，未在当前沙箱重跑浏览器；沙箱原启动限制是否改变不在本次结论中。
- 无页面横向溢出的量化结论来自原浏览器断言；静态截图不替代 Tab、Shift+Tab、Escape 或焦点恢复验证。
- 未进行完整 WCAG、屏幕阅读器、全浏览器矩阵、浏览器缩放、真实账号认证、后端权限、真实外部集成或生产部署验收。
- 这组截图与 smoke 不单独证明检索语义修复、治理阻断口径修复、看板计算口径或所有业务端到端流程；这些由父代理的模型/DOM/契约复核等证据覆盖。
- 父代理确认最新源码与引用证据一致后，可将“有界浏览器检查”判为通过；不能仅凭本报告将整个 AWW-12 或生产能力判为通过。
