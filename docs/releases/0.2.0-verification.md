# AwwO 0.2.0 验证记录

日期：2026-09-05。目标：独立的私有 Forgejo 仓库 `ClawHunt-Store/AwwO`，默认开发分支 `dev`，版本标签 `v0.2.0`。本记录在提交前按实际结果更新；没有把本地检查写成远端 CI 或生产部署结论。

## 当前实现

Agent 画布包含七种个性化组件模板、需求对话规划、可人工修改的节点与连线、独立 Session、标准输入输出表单、Codex 绑定、按依赖执行与运行恢复。节点收起后展示摘要，打开后提供会话、对话和交付物。源码及原有 Python、桌面和集成模块一并保留。

真实 Codex 七节点运行、十二条输入匹配、返工修复和原 Session 复验的记录见 [真实运行验证](../superpowers/2026-09-05-awwo-codex-live-verification.md)。这部分使用原来的隔离运行目录；移库没有重新生成或篡改该批交付物。可运行源码另整理到 [知识库示例](../../examples/knowledge-workbench/README.md)，其当前路径验证见 [示例验证](../../examples/knowledge-workbench/docs/verification.md)。

## 检查结果

| 范围 | 已验证结果 |
| --- | --- |
| 原交付分支 Web | 81 个测试文件，917 项通过；静态检查、类型检查和 development 构建通过 |
| 新 AwwO Gateway | 最终 33 个测试文件，354 项通过；类型检查和构建通过 |
| Gateway 锁定依赖 | Vitest 4.1.8，qs 6.16.0；生产及完整 npm audit 均为 0 项漏洞 |
| 新 AwwO Web | 锁文件修复后全新 `npm ci` 成功；最终 81 文件、928 项通过；类型、静态检查及 development 构建通过；完整 npm audit 0 项漏洞 |
| 开发入口 | 12 项脚本测试通过；完整 `npm run setup` 成功；独立端口 3101/8797/5189 健康检查通过；首次空库 125 项迁移成功；关闭后全部测试端口及自建 PostgreSQL 54330 释放，原有实例保持运行 |
| Python | 全量 5,896 项：5,728 通过、168 项既有跳过、0 失败；ruff、CLI doctor、隔离数据库 API `/health` 均通过 |
| 独立示例 | 模型 31、DOM 3、焦点 8 项通过；两项原始复现通过；1,585 条 Unicode 映射核验通过；1440/768/375/320px 实际浏览器检查通过 |
| 实际画布浏览器 | 新实例 HTTP 200，约 1.063 秒后可点击；七模板显示、前端节点添加通过；无未捕获页面异常，无旧 Python runtime/status 或 escalation 请求；没有提交 Agent 任务 |

执行环境：Windows，Node.js 22.16.0，npm 11.6.2，服务端 workspace 使用 pnpm 9.15.4，Python 3.12.10。新入口要求 Node 22.13+（22.x）或 24+。Python 使用项目内隔离虚拟环境；运行数据及本地证据存放在忽略的 `.local/` 中。

主要复验命令：`npm run setup`、`npm run test:scripts`、`npm test --prefix apps/web`、`npm test --prefix apps/gateway`、Gateway 目录的 `npm run typecheck` / `npm run build`、Web 目录的 `npx tsc --noEmit` 与 `node node_modules/vite/bin/vite.js build --configLoader runner --config vite.config.mjs --mode development`（设置 `APP_ENV=development`、`VITE_APP_ENV=development`）。两端分别执行完整和 `--omit=dev` 的 `npm audit --json`。Python 运行 `python -m ruff check packages apps`，并按 `scripts/run-ci-tests.sh` 原有文件划分完成先串行、后四 worker 的完整 pytest；没有新增跳过。

## 完整性与依赖修复

- 导出包含 12,323 个源文件，逐文件核对没有缺失；当前改动只涉及已列出的发布、依赖与便携入口内容。
- `server/` 没有源码修改，发布前核对 Git 树为 `729b741740efba9dae8807db58db7b730a8a0b93`，并恢复原来的可执行文件权限与两处符号链接模式。
- 最终 Git 暂存清单为 12,378 个文件，保留 69 处可执行权限和两处符号链接；137 份固定证据字节与工作区一致。
- 首次独立 `npm run setup` 检出了 Web 锁文件中 `@emnapi` 可选依赖不一致；普通锁文件修复后 `npm ci` 和完整 setup 通过，没有使用强制依赖修复。
- Python 首次全量的两项失败已修复：通过 `Path.home` 隔离 Windows 测试目录；取消传播测试按严格匹配的父子 ID 接受两条合法调度路径。生产 Python 未改动。完整复跑结果见上表，原红证据保留。
- Conversation API 在所有消息体解析器之前完成 socket loopback 与 token 校验；实际路由回归覆盖未授权畸形/超大请求、合法 JSON 错误和消息长度边界。
- 浏览器画布改为检查实际 Node 与 Gateway 的健康状态，并停止旧 Python 的启动与 escalation 轮询；上游不可达、八秒超时与迟到响应不会伪造成功，失败后可以重试。桌面与旧聊天模式保留原启动逻辑。首次截图在原逻辑的八秒兜底前仍有遮罩，最终用真实点击添加节点通过验收。
- 末次审查发现已有审批弹窗的跨页面残留，已改为画布不挂载旧审批组件。两条真实组件回归证明 dialog/badge 在进入画布后消失、停止轮询，回到旧工作台重新 GET 相同 pending，期间没有审批 POST 或状态变更。此跟进另经 Web 全量 928 项验证，没有重复计入早先的浏览器截图验收。
- `gcloud meta list-files-for-upload` 的实际本地清单核对为 5,890 个源文件，禁止运行态路径 0 个，Web 与 server 源码仍保留；未执行云端上传或部署。
- 一处历史文档中的 provider-key-shaped 值已在导出时替换为占位符。凭据、实时数据库、机器配置、依赖安装目录和原始运行日志不入库。第三方源代码与许可保留。

## 独立审查与发布状态

Codex gpt-5.5 已完成五轮只读对抗审查及针对修复的复审。上传排除规则 P1、消息解析顺序 P2、审批弹窗跨页面残留 P2 均已由真实独立 CLI 复审关闭；最终结论为 `COMPLETE REVIEW / PASS (Codex-only)`，剩余已验证 P0/P1/P2 为零。末次受审三个文件的 SHA256 前后一致，此前通过的启动及 Gateway 文件也已读回确认未变。继承的 `CLAUDE.md:140-166` 要求提交前 Codex 与 Gemini 均无阻断；Gemini CLI 使用现有授权配置返回 `403 / PERMISSION_DENIED / CONSUMER_SUSPENDED`，因此 Gemini 审查没有完成，也没有被记录为通过。操作者随后明确确认“全部执行推送”，批准本次按已通过的 Codex 审查提交发布；该决定仅记录本次例外，不把 Gemini 标为通过。

操作者已明确授权新建仓库和推送。Forgejo 仓库已创建并通过 API 读回：ID `218`、`ClawHunt-Store/AwwO`、私有、默认分支 `dev`，创建时为空；本地 `origin` 已指向该仓库。操作者已批准本次 Gemini 审查例外，发布采用工作分支上的首个快照提交，推送到 `dev` 并添加 `v0.2.0` 标签。实际 Git SHA 与远端标签读回记录随交付说明保存。

本地交付目录：`E:\Bobo's Coding cache\bo-work\AwwO`。当前工作分支：`codex/awwo-v0.2.0`，首个快照尚无父提交；来源为 `feat/awwo-agent-canvas`，原工作树基准 `feat/workflow-canvas-m1@c8db2818be40511bbb42ad5435028105a738e61c`。修改覆盖画布 Web、Gateway 边界、便携启动/示例、两处 Python 测试、依赖与发布文档；server 源码树未变。本次创建一个首版快照提交与 `v0.2.0` 标签，不创建 PR；操作者已明确批准本次 Gemini 审查例外。旧仓库 `main@f6d69d5f3858db92fd238554bdccef5265029d0b`、原有 `CLAUDE.md` 未提交修改及原服务均保留。

## 验证边界

本次验证覆盖本地开发入口、画布执行及演示交付物。示例的登录和数据仍是演示实现，真实身份服务、业务后端接入与生产发布未验收。旧 Python 成本、支付和其他兼容模块保留源码，需要单独启动相应服务；本次三服务启动不是这些模块的完整验收。可选插件 MCP bridge 未构建或验证，启用方法见开发指南；普通 Codex 主会话不依赖该可选 bridge。未重新打包桌面安装器，未运行真实支付或其他生产副作用。继承的工作流文件保留，但没有将其存在视为新 Forgejo 仓库 CI 已运行。
