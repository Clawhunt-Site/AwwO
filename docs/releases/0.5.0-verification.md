# AwwO 0.5.0 release verification

Released 2026-09-12 to the existing AwwO server acceptance environment. This is the Go/Pi SaaS application, not a desktop or legacy native release.

## Source and authorization

- Repository: https://git.clawhunt.store/ClawHunt-Store/AwwO
- Application commit: `edb7f3a26ecfaa08520a6f32fbb7b3c666de23c5`; base `cb96b76365444215da1e4ed4266c24b5f289ab08`.
- Worktree: `E:\Bobo's Coding cache\bo-work\AwwO-worktrees\artifact-selection-review`, detached throughout implementation. The code commit was pushed atomically to `main` and `online`; no additional named branch was created. Existing worktrees were preserved and the clean main/online mirrors fast-forwarded.
- Scope: 57 source/test/documentation/version files implement rendered HTML/Markdown deliverables, enlarged previews and bounded collaboration between selected independent Agent Sessions. See `docs/awwo-selection-collaboration-20260912.md` for behavior and limitations.
- Codex GPT-5.5 final review: PASS, no blocking findings, 381 reviewed source files. Behavior manifest SHA-256: `4322332eee1b9479819e71720357339b26aa0ea2bdf278282f972b8c379d0ee7`.
- After a specific question requesting a Gemini waiver for this 0.5.0 candidate, the operator replied “继续提交发布”. This release-specific waiver permits the commit and deployment despite the unavailable Gemini reviewer. OAuth returned `UNSUPPORTED_CLIENT`; the existing API credential returned 403 `CONSUMER_SUSPENDED`. Gemini did not pass review. Future dual-review requirements remain unchanged.

## Validation

- Final affected frontend suite: **35 files, 358 tests passed**; SaaS TypeScript and build passed.
- Broader frontend canvas/SaaS tests: 1,147/1,149 passed. The two native `canvas-run-entry.test.tsx` failures were reproduced using the complete HEAD frontend source/tests/config archive with identical installed dependencies. All 317 archived Git blobs matched HEAD. They remain baseline failures in that environment, not passing checks.
- Isolated PostgreSQL suite passed; final atomic collaboration regression passed; durable message metadata regression and vet passed. Tests verify actual turn identities, tenancy, original Sessions, bounded calls, partial/final boundaries, cancellation, interrupted recovery and atomic artifact/graph completion.
- Real browser: rendered HTML/Markdown, source/download consistency, boxed selection, directional handoffs, running refresh without redispatch, cancellation retaining partial output, source disclosure, desktop enlargement and 390×844 preview.
- Stored HTML fixture: original/API/browser source all 4,421 bytes, identical SHA-256. Script, navigation and network probes remained inert; controlled network log recorded zero fixture requests. These are static previews, not execution of scripts embedded in generated apps.
- Real provider: `qwen3.8-27b` through Pi performed exactly five successful calls in the original two Sessions: two proposals, two critiques, one final HTML synthesis. Actual peer outputs and history were read back. This was isolated local model acceptance, not a new model run on the shared server. The test services were stopped after confirming zero active calls; receipts/data preserved.

## Deployment and independent readback

- AWS: `us-east-2`, instance `i-0eda5599cbd603c8b`.
- URL: https://awwo.clawhunt.store/ ; staging behind Cloudflare Access.
- Release: `/srv/awwo/releases/saas-edb7f3a`.
- Runtime API revision: `edb7f3a26ecfaa08520a6f32fbb7b3c666de23c5`.
- API binary SHA-256: `b0ba70dd029a597b1310c2f6414502d671ce5f7fbb1adea8b73a8942b93e4434`.
- Uploaded archive SHA-256: `efee8fc2afc09554be64ef2ed79262c368505e59d93d65e08c926acd024642eb`.
- The dedicated `awwo-saas-web` ingress was briefly stopped, all existing tasks verified idle, then only the SaaS API and PostgreSQL were stopped for backup. The old native services and port 54329 database were not stopped or modified.
- Fresh cold backup: `/srv/awwo/saas-staging/backups/release-050-20260912T112734Z/postgres-data.tar`, **68,229,120 bytes**, SHA-256 `1dda9ea63a867f13eeadba847694ed8deca3db4587b9377c750809915f3d38ce`. Tar contents were compared to the stopped data. Independent readback read all 1,427 members and confirmed PG18 ownership/modes. This is a verified archive, not a performed database restore.
- SaaS PostgreSQL remains **55483 / awwo / public / PG18.1**. Migrations **1–11** are recorded; collaboration table, columns and foreign keys independently verified. New binary startup performed additive migration11 transactionally.
- Existing API/Pi environment files and their values were preserved. Pi remains the existing `saas-48b96d7` runtime; its process identity and the native services were unchanged.
- API health, running executable hash, uploaded binary hash, nginx root, actual HTML after the unchanged STAGING injection, and asset manifest independently match the release. Four unauthenticated API routes return401; noindex and the STAGING marker remain. Final active graph, invocation, member and Pi counts were zero.
- Deployment script independently reviewed after fixing three activation/recovery hazards. Final script SHA-256: `ed96190a687ee54e8aaf9714fe072a2b1240433ad564b1eef3fa41ddc237ed9e`.

## Browser and operational limits

The public browser reached Cloudflare Access's login page, confirming the outer gate is present. The current browser has no authenticated Access session, so the authenticated public canvas path is not claimed as verified. Through the authorized SSH loopback connection, the deployed server login page and STAGING marker rendered correctly. The local forwarded hostname is not an allowed API origin; no credential was copied, CSRF protection changed or login attempted through that hostname.

The old release and all durable data are retained. Recovery can revert the API executable and web root after confirming idle state while preserving additive migration11. No database restore/drop, model replay or fixture seeding was performed on the server. Do not automatically restore the pre-release database after users resume writing.

Evidence remains in the task's ignored `.local/review-acceptance/` directory: `release-build-receipt.json`, `release-activation.log`, `server-postdeploy-readback.json`, `server-login-deployed.png`, final frontend/baseline reports and real-provider receipts. The server receipt is `/srv/awwo/saas-staging/evidence/release-050-20260912T112734Z.json`. Later documentation-only commits do not change the deployed application revision stated above.
